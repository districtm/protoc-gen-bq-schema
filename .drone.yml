---
kind: pipeline
type: docker
name: default

steps:
  - name: cr
    image: plugins/docker
    settings:
      username: chuhlomin
      password:
        from_secret: docker_password
      tags:
        - ${DRONE_COMMIT}
        - latest
    when:
      event: push
      branch: develop

trigger:
  branch:
    - develop

image_pull_secrets:
  - dockerconfigjson
